# Google Gemini API Key
# Used for live AI generation. If not provided, the app gracefully degrades to local fallback data.
# Note: In production, this key should be proxied through a backend (e.g. Firebase Functions or Cloud Run)
VITE_GEMINI_API_KEY=your_gemini_api_key_here
VITE_FIREBASE_PROJECT_ID=wayshift-engine
